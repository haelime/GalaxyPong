{
  "name": "galaxy-pong",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "corepack pnpm --filter @galaxy-pong/shared build && corepack pnpm --parallel --filter @galaxy-pong/api --filter @galaxy-pong/web dev",
    "build": "corepack pnpm -r build",
    "lint": "corepack pnpm -r lint",
    "typecheck": "corepack pnpm --filter @galaxy-pong/shared build && corepack pnpm -r typecheck",
    "test": "corepack pnpm --filter @galaxy-pong/shared build && corepack pnpm --filter @galaxy-pong/api prisma:generate && corepack pnpm -r test",
    "test:e2e": "corepack pnpm --filter @galaxy-pong/web test:e2e",
    "db:migrate": "corepack pnpm --filter @galaxy-pong/api db:migrate",
    "db:seed": "corepack pnpm --filter @galaxy-pong/api db:seed"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
